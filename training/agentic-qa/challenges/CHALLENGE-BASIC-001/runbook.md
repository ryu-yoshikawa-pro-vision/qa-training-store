# Basic Web Exploration

Use the supplied learner specification and the suspended-account seed to exercise the sign-in flow.

Record the user-visible result, current URL, and a screenshot. Continue until the required coverage is complete or the exploration budget is exhausted. Findings must describe one atomic observation at a time and distinguish expected behavior from actual behavior.

Do not inspect source code, repository files, tests, patches, answer keys, or external search results.
