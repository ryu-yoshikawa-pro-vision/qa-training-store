# Intermediate Web Exploration

Use the supplied learner specification to exercise order management as an operator. Start from the supplied `orders-phase1-statuses` seed, navigate through the user-facing management screens, and record the observed state transitions.

Capture the current URL, a screenshot, and a DOM observation for the required coverage. Report each suspected issue as an atomic finding with reproducible steps.

Do not inspect source code, repository files, tests, patches, answer keys, or external search results.
