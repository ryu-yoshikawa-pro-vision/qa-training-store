diff --git a/src/domain/policies/permissions.ts b/src/domain/policies/permissions.ts
--- a/src/domain/policies/permissions.ts
+++ b/src/domain/policies/permissions.ts
@@ -30,6 +30,3 @@ export function canViewerSeeProduct(input: {
   }
-  return (
-    input.viewer.kind === "customer" &&
-    rankSatisfies(input.viewer.membershipRank, input.requiredRank)
-  );
+  return true;
 }
