# Plan

## Workflow

- Level: Lightweight
- Run ID: `20260727-192924-JST`
- Scope: Web版のスクロール可否と原因のread-only診断

## 仮説

- 画面ルートまたはWeb専用CSSに `overflow: hidden` 相当があり、ビューポートより長い画面でもスクロール領域が生成されていない。
- ページ全体ではなく内側の `ScrollView` がスクロール対象で、ブラウザ上で操作対象が分かりにくい。

## 完了条件

- デスクトップWebで長い代表画面を開き、スクロール前後の位置を実測する。
- 関連するレイアウト実装を確認し、再現条件と原因をユーザーへ回答する。
- 診断のみとし、修正はユーザーの明示依頼を待つ。
