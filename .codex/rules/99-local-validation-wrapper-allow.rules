# Windows may expose Node as node.exe. The action/argument contract is
# enforced by scripts/codex-local-validation.mjs, not by prefix_rule.

prefix_rule(
  pattern = ["node.exe", "scripts/codex-local-validation.mjs"],
  decision = "allow",
  match = ["node.exe scripts/codex-local-validation.mjs verify"],
)
