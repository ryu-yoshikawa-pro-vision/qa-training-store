# Parent-defined validation is exposed through one Node dispatcher.
# prefix_rule is intentionally not treated as exact matching; the dispatcher
# rejects unknown actions and extra arguments before starting any validation.

prefix_rule(
  pattern = ["node", "scripts/codex-local-validation.mjs"],
  decision = "allow",
  match = ["node scripts/codex-local-validation.mjs verify"],
)
