# PR #58レビュー指摘完全対応計画

## 0. 依頼概要

- 依頼内容: PR #58のbrace-expansion remediationを変更せず、Issue #54、PR本文、Run Artifact、CI evidenceの矛盾を修正する。
- 背景: Android Automation Build failureとNative Static failureがあるのに、Maestro成功やnative-ci派生failureのみと誤って記録されていた。
- 期待成果: 同一headのfailed jobsを1回だけrerunし、最終CIをGitHub metadataへ記録してPRのmerge可否を判断する。

## 1. ゴール / 完了条件

- ゴール: 第三者がPR #58を見ても、R2採用条件、local validation、CIの実行範囲、独立failure、merge可否を誤解しない状態にする。
- 完了条件（DoD）:
  - `package.json` / `pnpm-lock.yaml`のR2差分が不変。
  - Issue #54 DoDにmetadata-only許容条件が明記される。
  - 前Run Artifactの最新CI記録が7a20fdebの実態と一致し、CI記録終端ルールが明記される。
  - PR本文がAndroid Runtime / Maestro jobと実Maestro flowを区別する。
  - Artifact修正をcommit/pushし、push後の新headでCIを確認する。
  - 同一headのfailed jobsを1回だけrerunし、結果をGitHub metadataへ記録する。
  - PRはmergeせず、nanoidへ進まない。

## 2. 現状理解と前提

- Current understanding:
  - branch HEADは`7a20fdeb786339086023383e27affc15bca40e5b`。
  - R2は`minimatch@3.1.5>brace-expansion -> 1.1.18`、`minimatch@10.2.5>brace-expansion -> 5.0.9`。
  - 既存Mobile App CI `32734755542`はNative Static failure、Android Automation Build failure、`native-ci / verify` failure。
  - Android Runtime / Maestro jobはsuccessだが、Android Automation Build failureによりAutomation APK依存flowはskip。
- Assumptions: 最後のArtifact commit以降のCIはRun Artifactへ追記せず、PR/Issue metadataを正本とする。
- Non-goals: dependency再設計、Expo/Gradle修正、source/test/workflow変更、merge、nanoid対応。

## 3. 質問 / 曖昧性

- 必ず質問する不透明点: なし。
- 仮定してよい細部: rerunはGitHub Actionsのfailed jobsのみを1回実行する。
- 未回答の重要質問: rerun後にFoojay failureが再現するか、Expo Doctor failureが継続するか。

## 4. 影響範囲

- Impacted areas: `.codex/runs/20260824-202628-JST/`、今回Run Artifact、Issue #54、PR #58、必要時の独立CI Issue。
- Files to inspect: `package.json`、`pnpm-lock.yaml`、前Runの`REPORT.md`/`evaluation.json`/`run.json`、`.github/workflows/native-ci.yml`。

## 5. 変更方針

- 依存ファイルは変更しない。
- 前Run REPORTはappend-onlyで訂正追記し、既存履歴を削除しない。
- `evaluation.json`と`run.json`は最新head/jobの事実とfailure分類へ同期する。
- Issue本文のDoD、PR本文、Issue commentをGitHub metadataとして更新する。
- 独立failureをPRへ混ぜず、同一headで再現した場合は別Issueへ切り出す。
- CI結果をArtifactへ書き戻すためだけの追加commitは作成しない。

## 6. 検証方法

- `git diff origin/main...HEAD -- package.json pnpm-lock.yaml`でR2差分の不変を確認。
- Node/pnpm、frozen install、why/list、audit、format、verify、diff check、必要ならlockfile no-opを再確認。
- `scripts/sanitize-codex-artifacts.ps1 -Path ... -Write/-Check`、evaluation schema/linkage、markdown lintを実行。
- `gh pr checks`、`gh run view --jobs`、failed job logでjob結論と実flow実行を分離確認。
- rerun後のCI結果をIssue/PR metadataへ記録し、PR state/merge state/Alert stateを再確認。

## 7. リスクと未解決論点

- Artifact修正commit自体が新CIを発生させるため、以後のCIをArtifactへ反映する無限ループを禁止する。
- Android Automation Buildが一時的に成功しても、Maestro各flowが実行されたことをログで確認できなければPASSと扱わない。
- Expo patch mismatchやFooJay plugin resolutionはR2と独立であり、今回のPRへ依存更新を追加しない。

## 8. 成果物

- Run Artifactの訂正・評価、今回Run Artifact、durable plan。
- Issue #54本文/comment、PR #58本文/comment。
- 同一headで再現した独立CI failureのIssue（必要時）。

## 9. 備考

- 最終判断は`READY TO MERGE`または`NOT READY TO MERGE`を明示し、merge操作は実施しない。
